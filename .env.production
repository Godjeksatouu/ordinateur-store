# Production Environment Configuration for ordinateurstore.ma

# Database Configuration
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=store

# Email Configuration (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=your_email@gmail.com
MAIL_PASS=your_app_password
MAIL_FROM=your_email@gmail.com

# JWT Secret for authentication (IMPORTANT: Generate a strong secret)
JWT_SECRET=your_very_strong_jwt_secret_here_minimum_32_characters

# Backend Server Port
BACKEND_PORT=5000

# Frontend URL (Production domain)
FRONTEND_URL=https://ordinateurstore.ma
NEXT_PUBLIC_API_BASE_URL=https://ordinateurstore.ma

# Server Configuration
PORT=3000
HOSTNAME=0.0.0.0

# Node Environment
NODE_ENV=production
